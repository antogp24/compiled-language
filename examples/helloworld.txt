struct Rect2D {
    top_left: vec2,
    size: vec2,
}

struct Player {
    collider: Rect2D,
    hp: i32,
    texture_id: u32,
}

enum Test_Enum { A, B, C, D }

fn test() {
    #'some_label: loop {
        loop {
            break some_label;
        }
    }

    let xs: []i32 = {0, 1, 2, 3};
    let ptr_to_second = cast(*i32)&xs.data[2]; 

    a = b = c; // TODO: Associativity is wrong here. I have to fix the parser to handle right-to-left.

    let player = &players[0];
    let alive_after_damage = damage(player, 5);

    // single line comment
    let a = 0xff * 0b10 + 6 - 7 - 0o17 / 2;
    let b = '\n' + 69.420;
    print("hello world");
    print(a + b);
}

let players: [1]Player = {
    { .hp = 65 },
};

fn damage(self: *Player, value: int) -> bool {
    self.hp -= value;
    self.hp = max(self.hp, 0);
    return self.hp > 0;
}

fn main() {
    if alive_after_damage {
        println("Player is alive");
        exit(0);
    } else {
        println("Player was killed");
        exit(1);
    }
}

/*
 * multiline comment
 */

/*
    /*
     * nested multiline comment
     */
 */
